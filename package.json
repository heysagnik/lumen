{
  "name": "lumen",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm:dev:api\" \"npm:dev:web\"",
    "dev:web": "npm run dev --workspace=@lumen/web",
    "dev:api": "npm run dev --workspace=@lumen/api",
    "build": "npm run build --workspace=@lumen/shared --if-present && npm run build --workspace=@lumen/api && npm run build --workspace=@lumen/web",
    "lint": "npm run lint --workspace=@lumen/web && npm run lint --workspace=@lumen/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
